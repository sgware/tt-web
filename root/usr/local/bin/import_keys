#!/bin/sh

# Import the server's public and private keys into a Java keystore.
openssl pkcs12 -export \
  -in /etc/ssl/certs/tt-fullchain.pem \
  -inkey /etc/ssl/private/tt-private.pem \
  -out /opt/tt/tt-keystore.p12 \
  -name tt \
  -caname root \
  -passout pass:changeit
